module.exports = {
  plugins: {
    // Tailwind v4 moved the PostCSS plugin into a separate package.
    // Use the package name the scaffold installed: @tailwindcss/postcss
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};
